### An accordion component created using React and styled with vanilla CSS

#### Available Scripts:
- npm start - To start the project on a development start.
- npm build - To build the project for production
